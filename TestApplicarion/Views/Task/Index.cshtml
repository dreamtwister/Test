@model TestApplicarion.Models.TasksPagingDTO

@{
    ViewBag.Title = "Задачи";
}

@foreach (var task in Model.Tasks)
{
    <div class="item">
        <h3>@task.Name</h3>
        @if (task.IsDone)
        {
            <div>Выполнено</div>
        }
        else
        {
            <div>Невыполнено</div>
        }
    </div>
}